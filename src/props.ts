export default class Props {
  constructor(
    public inverse: boolean,
    public ignoreSpaces: boolean,
    public spacesToAdd: number,
    public allCaps: boolean,
    public allLow: boolean
  ) {}
}
